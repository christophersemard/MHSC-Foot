<form method="post" action="{{ path('app_staff_crud_delete', {'id': staff.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette catégorie ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ staff.id) }}">
	<button class="dropdown-item text-danger">Supprimer</button>
</form>
