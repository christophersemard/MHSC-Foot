{% extends 'admin_base.html.twig' %}

{% block title %}Equipe
{% endblock %}

{% block body %}
	<main class="p-5 w-100">
		<h1>Equipe</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ team.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ team.name }}</td>
				</tr>
			</tbody>
		</table>

		<a href="{{ path('app_team_crud_index') }}">Retour à la liste</a>

		<a href="{{ path('app_team_crud_edit', {'id': team.id}) }}">Modifier</a>

		{{ include('team_crud/_delete_form.html.twig') }}
	</main>
{% endblock %}
