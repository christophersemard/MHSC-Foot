<form method="post" action="{{ path('app_player_crud_delete', {'id': player.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette catégorie ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ player.id) }}">
	<button class="dropdown-item text-danger">Supprimer</button>
</form>
