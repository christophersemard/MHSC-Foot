<form method="post" action="{{ path('app_post_category_crud_delete', {'id': category.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cette catégorie ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
	<button class="dropdown-item text-danger">Supprimer</button>
</form>
